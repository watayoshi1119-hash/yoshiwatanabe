---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';

const posts = (await getCollection('blog')).sort(
  (a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime()
);

const categories = [...new Set(posts.map(p => p.data.category))];
---
<BaseLayout title="記事一覧">
  <style>
    .category-nav {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      margin-bottom: 2rem;
    }
    .category-btn {
      padding: 0.3rem 0.9rem;
      border: 1px solid #ccc;
      border-radius: 20px;
      background: #fff;
      color: #555;
      font-size: 0.85rem;
      text-decoration: none;
      transition: all 0.2s;
    }
    .category-btn:hover, .category-btn.active {
      background: #333;
      color: #fff;
      border-color: #333;
    }
    .posts {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
      gap: 1.5rem;
    }
    .card {
      background: #fff;
      border: 1px solid #e8e8e4;
      transition: box-shadow 0.2s, border-radius 0.3s;
    }
    .card:nth-child(1) { border-radius: 72% 28% 60% 40% / 35% 68% 32% 65%; }
    .card:nth-child(2) { border-radius: 30% 70% 42% 58% / 68% 28% 72% 32%; }
    .card:nth-child(3) { border-radius: 58% 42% 25% 75% / 45% 72% 28% 55%; }
    .card:nth-child(4) { border-radius: 25% 75% 65% 35% / 72% 38% 62% 28%; }
    .card:nth-child(5) { border-radius: 68% 32% 38% 62% / 28% 58% 42% 72%; }
    .card:nth-child(6) { border-radius: 42% 58% 72% 28% / 62% 32% 68% 38%; }
    .card:hover { box-shadow: 0 6px 20px rgba(0,0,0,0.1); }
    .card a { text-decoration: none; color: inherit; display: block; padding: 1.4rem 1.6rem; }
    .card-category {
      font-size: 0.75rem;
      color: #888;
      margin-bottom: 0.5rem;
      letter-spacing: 0.05em;
    }
    .card-title {
      font-size: 1rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
      line-height: 1.5;
    }
    .card-desc {
      font-size: 0.85rem;
      color: #666;
      line-height: 1.6;
    }
    .card-date {
      font-size: 0.75rem;
      color: #aaa;
      margin-top: 0.8rem;
    }
  </style>

  <div class="category-nav">
    <a href="/" class="category-btn active">すべて</a>
    {categories.map(cat => (
      <a href={`/category/${cat}`} class="category-btn">{cat}</a>
    ))}
  </div>

  <div class="posts">
    {posts.map(post => (
      <div class="card">
        <a href={`/blog/${post.id}`}>
          <div class="card-category">{post.data.category}</div>
          <div class="card-title">{post.data.title}</div>
          <div class="card-desc">{post.data.description}</div>
          <div class="card-date">{post.data.date}</div>
        </a>
      </div>
    ))}
  </div>
</BaseLayout>
